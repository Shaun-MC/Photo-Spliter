Bugs
    Drag & Drop
        - Photo doesn't get inserted into 

ToDo
    Create a Flask backend that exposes APIs that handle the S3 bucket access
    
    Write the python scripts that are called from the imageProcessing.ts file that handles S3 & lambda access code execution
    
    Add Saturation, Hue, Brightness photo storing

    Double check allowed memebers can check out the repo

    Make sure the S3 bucket acess is only local to this repo